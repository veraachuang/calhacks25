# ——— Lens Studio ———
# Keep scene/ and entities/ in source control. Ignore local caches & backups.
Cache/
Backup/
# Optional: ignore project.data (binary cache). Add back if you want faster recompiles.
project.data
# Per-user settings files (4.x and 5.x)
*.lsproj.user
*.esproj.user
# Binary exports you may generate from tooling
*.lso

# If you add custom heavy previews and don't want them in git, uncomment:
# Previews/

# ——— Python ———
# Virtual environments
venv/
env/
ENV/
.venv/
.env/

# Python compiled files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Distribution / packaging
.buildozer/
.eggs/
*.egg-info/
.installed.cfg
*.egg
wheels/
dist/
build/
develop-eggs/
downloads/
eggs/
lib/
lib64/
parts/
sdist/
var/
pip-wheel-metadata/

# Pip
pip-log.txt
pip-delete-this-directory.txt

# PyInstaller
*.manifest
*.spec

# Unit test / coverage
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# ——— Node / Web ———
node_modules/
npm-debug.log*
yarn-error.log*

# ——— IDEs / Editors ———
.vscode/
.idea/
*.swp
*.swo
*~

# ——— Environment files ———
.env
.env.local

# ——— SSL Certificates (Video Chat) ———
cert.pem
key.pem

# ——— Session Data ———
sessions/*.json
sessions/log.csv

# ——— Profile Data (Optional: keep base profiles in git if desired) ———
# Uncomment the line below to ignore profile files:
# profiles/*.json

# ——— OS junk ———
.DS_Store
Thumbs.db
